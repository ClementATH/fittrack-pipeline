# Freshness rules are defined in config/quality_rules.yaml
# This file exists as a placeholder for future per-source overrides.
# See config/quality_rules.yaml -> freshness_rules for the active rules.
